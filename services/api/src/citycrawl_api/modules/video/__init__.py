"""Video extension point. The router and versioned models exist, but no processing endpoint
reports success. Actual execution, storage writes, limits, and failure semantics require a
separate design when the processor exists."""
