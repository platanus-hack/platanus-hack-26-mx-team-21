# Vialia City Priority Map — web app environment (copy to apps/web/.env, never commit real keys)

# Supabase project (remote dev project joixzhdpnxqhnuscxsoy)
VITE_SUPABASE_URL=https://joixzhdpnxqhnuscxsoy.supabase.co
# Publishable/anon key — get it from `supabase` dashboard → Project Settings → API,
# or `supabase status` for a local stack. (Safe to ship in the browser; still not committed here.)
VITE_SUPABASE_ANON_KEY=__set_me__

# CityCrawl modular API (Fly) — planning + natural-language draft parsing.
# Local dev: http://localhost:8090 ; production: https://citycrawl-api.fly.dev
VITE_CITYCRAWL_API_URL=http://localhost:8090

# ---------------------------------------------------------------------------
# Dev login users seeded by supabase/seed/* — password for ALL: vialia-dev-2026!
#   author.a@vialia.test    role=analysis_author   (full app: run/cancel analyses)
#   viewer.a@vialia.test    role=viewer            (read-only)
#   nomember@vialia.test    role=none              (no membership → §5 empty-state test)
# Tenant: "Vialia CDMX". Boundary covers 6 alcaldías (Tlalpan deliberately excluded
# so its observations exercise the geo-clip). ~132 observations, 4 pre-seeded analysis runs.
# ---------------------------------------------------------------------------
